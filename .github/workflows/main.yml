name: Linux Self Hosted Workflow

on: push

jobs:
  build:
    runs-on: ubuntu01
      - self-hosted
      - linux

    steps:
      - name: Display Message
        run: echo "Running on Linux Self-Hosted Runner"
